var mongoose = require('mongoose');

//Schema sources info from users Google profile
var essayQuestionsSchema = mongoose.Schema({

});
module.exports = mongoose.model('EssayQuestions', essayQuestionsSchema);
